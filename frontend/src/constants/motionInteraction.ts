/** Bottom nav and matching pill / FAB icon hover (MainLayout). */
export const navIconButtonMotion = {
  whileHover: { scale: 1.08, rotate: 2 },
  whileTap: { scale: 0.94, rotate: -10 },
} as const;
